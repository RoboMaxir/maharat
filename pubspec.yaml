name: iranian_freelance_ai
description: A compact on-device AI system for Iranian freelancers with full Persian support.
version: 1.0.0
homepage: https://github.com/your-username/iranian-freelance-ai

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true